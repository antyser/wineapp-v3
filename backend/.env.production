# Production Environment Configuration
ENVIRONMENT=production

# API Configuration
API_V1_STR=/api/v1
PROJECT_NAME="Wine App API"

# Supabase Production Configuration
# These are placeholder values - replace with actual production credentials
SUPABASE_URL=https://your-production-project.supabase.co
SUPABASE_ANON_KEY=your-production-anon-key
SUPABASE_SERVICE_KEY=your-production-service-key

# CORS - Update with production origins
BACKEND_CORS_ORIGINS=["https://your-production-domain.com","https://www.your-production-domain.com"]

# JWT - Use strong secret in production
SECRET_KEY=replace_with_strong_production_secret_key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080  # 7 days 